Currently, Uncivilization is set up as a package.
Using pip for python3 (3.6+), simply run pip install -e /path/to/Uncivilization
(setup.py should exist there)

The setup file has a console script, just run Unciv in terminal
to start the game.
